fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pinned: passlib 1.7.4 has a known incompatibility with bcrypt>=4.1
# (raises on version detection). 4.0.1 is the last version confirmed to
# work cleanly with passlib's bcrypt handler.
bcrypt==4.0.1
redis==5.0.8
python-multipart==0.0.9
alembic==1.13.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
fakeredis==2.24.1
